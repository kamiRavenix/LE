# 常见网络协议（按 TCP/IP 四层模型分类）

## 网络接口层（链路层）

1. **Ethernet（以太网）**：局域网底层帧协议，网卡之间传输，Wireshark 最第一层就是以太网帧，包含 MAC 源 / 目的地址。
2. **ARP**：地址解析协议，**IP → MAC**；过滤器：`arp`。
3. **RARP**：反向地址解析，MAC→IP，现在很少用。

## 网际层（网络层）

1. **IP（IPv4）**：互联网核心，源 IP、目的 IP，分片。
2. **ICMP**：互联网控制报文协议。ping 命令就是 ICMP；过滤器`icmp`。
   - 类型：请求回显、应答、目标不可达、超时。
3. **IGMP**：组播管理协议，视频直播组播使用。

> 
> 网络层没有端口概念，端口是传输层才有。

## 传输层（端口在这里）

1. **TCP**：面向连接，可靠，三次握手、四次挥手。端口示例：80 (HTTP)、443 (HTTPS)、22 (SSH)、21 (FTP)。
2. **UDP**：无连接，不可靠，没有握手挥手，速度快。端口：53 (DNS)、69 (TFTP)、161 (SNMP)。

> 
> 对比记忆：TCP 重传、流量控制 (Win 窗口)、序列号 Seq/Ack；UDP 啥都没有，只管发。

## 应用层（上层业务协议，跑在 TCP/UDP 之上）

### 跑在 TCP 上

- **HTTP**：80 端口，网页明文；过滤器 `http`
- **HTTPS(TLS/SSL)**：443 端口，加密网页；TLS 负责加密，SNI 携带域名。过滤器`tls`
- **FTP**：21 控制端口，20 数据端口，文件传输
- **SSH**：22 端口，远程登录加密
- **Telnet**：23 端口，明文远程登录（不安全）
- **SMTP**：25，发邮件
- **POP3**：110，收邮件

### 跑在 UDP 上

- **DNS**：53 端口，域名解析（域名↔IP），大部分 UDP；大报文会走 TCP。过滤器`dns`
- **DHCP**：67/68，自动分配 IP 地址。过滤器`dhcp`
- **SNMP**：网络设备监控
- **TFTP**：简单文件传输
